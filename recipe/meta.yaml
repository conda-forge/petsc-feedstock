{% set build = 1 %}
{% set version = '3.8.1' %}
{% set sha256 = 'fdc94525422c41ed617b38d09723bb27c6dbf4831c4c6cc1e27850e60788ccfa' %}
{% set blas = os.environ.get('BLAS') or 'openblas' %}
{% set openblas_version = '0.2.20' %}
{% set mpi = os.environ.get('MPI') or 'mpich' %}
{% set req = {'mpich':'>=3.2', 'openmpi':'>=1.10'} %}

package:
  name: petsc
  version: {{version}}

source:
  fn: petsc-{{version}}.tar.gz
  url: http://ftp.mcs.anl.gov/pub/petsc/release-snapshots/petsc-lite-{{version}}.tar.gz
  sha256: {{sha256}}

build:
  skip: true  # [win]
  number: {{build}}
  binary_relocation: true
  detect_binary_files_with_prefix: true
  features:
    - blas_{{blas}}

requirements:
  build:
    - toolchain
    - python 2.7.*
    - cmake
    - blas 1.* {{blas}}
    {% if blas == 'openblas' %}
    - openblas {{openblas_version}}
    {% endif %}
    - {{mpi}} {{req[mpi]}}
    - ptscotch
    - suitesparse 4.5.4
  run:
    - blas 1.* {{blas}}
    {% if blas == 'openblas' %}
    - openblas {{openblas_version}}
    {% endif %}
    - {{mpi}} {{req[mpi]}}
    - ptscotch
    - suitesparse 4.5.4

test:
  source_files:
    - src/snes/examples/tests
  commands:
    - conda inspect linkages petsc  # [not win]

about:
  home: http://www.mcs.anl.gov/petsc/
  summary: 'PETSc: Portable, Extensible Toolkit for Scientific Computation'
  license: BSD 2-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - dalcinl
    - minrk
